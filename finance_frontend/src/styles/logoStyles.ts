import { SxProps, Theme } from '@mui/material';

export const logoTypographyStyle: SxProps<Theme> = {
  fontFamily: "'Poppins', sans-serif",
  fontWeight: 'bold',
  letterSpacing: 1,
  textShadow: '1px 1px 1px rgba(0,0,0,0.1)',
};
